from selene import browser, have
from appium.webdriver.common.appiumby import AppiumBy


def test_sample_app_ios(ios_management):
    # browser.element((AppiumBy.ACCESSIBILITY_ID, "Setting")).click()
    # browser.element((AppiumBy.ACCESSIBILITY_ID, "1.3.0")).should(have.text("1.3.0"))
    # browser.element((AppiumBy.CLASS_NAME, "XCUIElementTypeStaticText")).should(have.text(0))
    """
    Здесь должен быть код теста
    """